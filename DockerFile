# Dockerfile
# Use an official Python runtime as a parent image
FROM python:3.9-slim

# Set the working directory in the container
WORKDIR /app

# Copy the requirements file and install dependencies
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Copy the rest of the application code
COPY . .

# Expose the port the app runs on
EXPOSE 5000

# Run the application
# We use Gunicorn with SocketIO for production-ready deployment
# This is a good practice as Flask's built-in server is for development only.
# To run with the development server, you would use `CMD ["python", "app.py"]`
CMD ["gunicorn", "--bind", "0.0.0.0:5000", "--worker-class", "eventlet.wsgi.server", "app:app"]